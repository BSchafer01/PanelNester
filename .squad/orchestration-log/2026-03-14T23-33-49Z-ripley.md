# Ripley Orchestration Log — Phase 5 Follow-Up Kickoff

**Date:** 2026-03-14T23:33:49Z  
**Role:** Lead (analysis and ownership split)  
**Mode:** sync

## Spawn Directive

Analyze Brandon's Phase 5 follow-up correction notes and split into exact agent ownership and seam boundaries.

## Outcome

✅ **Analysis Complete**

### Root Causes & Ownership Split Confirmed

| Issue | Root Cause | Owner | Layer | Work Items |
|-------|-----------|-------|-------|-----------|
| **Viewer uses SVG, not Three.js** | Dallas shipped SVG as interim (Phase 5 decision). User now requires Three.js explicitly. | Dallas | WebUI | Replace SVG with Three.js `OrthographicCamera` + `OrbitControls` (rotation locked). Add `max-height` constraint to `.sheet-viewer`. |
| **Viewer grows unbounded** | `.sheet-viewer` has `min-height: 320px` and `height: 100%` with no upper bound. | Dallas | WebUI | Add `max-height` (suggest 480–520px). |
| **Mouse input not locked to viewer** | Current SVG uses `preventDefault()` and pointer capture; Three.js `OrbitControls` provides native mouse containment. | Dallas | WebUI | OrbitControls capture happens automatically. No custom viewport math needed. |
| **PDF report missing panel labels** | `QuestPdfReportExporter.BuildSheetSvg()` renders rectangles only, never adds `<text>` elements. `SheetViewer.tsx` UI renders labels; exporter does not. | Parker | Services/Exporter | Add `<text>` labels to `BuildSheetSvg()` for each placement's `PartId`. |
| **PDF utilization shows ~6000% instead of ~60%** | `FormatPercent()` uses `value.ToString("P1")`. .NET "P" format multiplies by 100; but `ShelfNestingService.ToPercent()` already multiplied. Double-multiply bug. | Parker | Services/Exporter | Change `FormatPercent()` from `value.ToString("P1")` to `$"{value:0.0}%"`. |

### No Bridge or Host Changes Needed
- `SheetViewer` receives `NestSheet` and `NestPlacement[]` as immutable data props.
- Rendering tech (SVG → Three.js) does not change data contract.
- Bishop has no work.

## Decisions Recorded

1. **Viewer must be Three.js** — Not optional. Phase 5 design spec called for it; shipping SVG was interim. Now explicit.
2. **Three.js with rotation lock** — Use `OrthographicCamera` + `OrbitControls` (set `autoRotate: false`, restrict to 2D-plane navigation).
3. **Viewer height must be constrained** — Prevents domination on large layouts.
4. **Report formatting is exporter-only** — No data shaping layer changes. Labels and percentages are rendering concerns, not domain model changes.

## Next Steps

- Dallas: Begin Three.js viewer implementation (no blocking dependencies).
- Parker: Begin PDF label and percentage fixes (no blocking dependencies).
- Hicks: Draft acceptance gate for follow-up batch (verify user-visible evidence in viewer and PDF).
- Ripley: Authorization for parallel correction batches begins immediately.

**Phase 5 Correction Cycle: AUTHORIZED 2026-03-14T23:33:49Z**
