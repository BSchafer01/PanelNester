# Orchestration Log — Hicks Phase 4 Gate Scaffolding

**Timestamp:** 2026-03-14T18:34:43Z  
**Role:** Tester — Acceptance Criteria & Regression Coverage  
**Mode:** sync  
**Status:** COMPLETE

## Why Chosen

Proactive Phase 4 gate scaffolding from the PRD while design review ran. Hicks establishes testing roadmap, gating criteria, and test matrix structure concurrent with Ripley's scope definition to avoid divergence between design decisions and verifiable acceptance.

## Phase 4 Test Gate Definition

**Decision: Hicks Decision — Phase 4 Test Gate**

### Four Non-Negotiable Review Gates:
1. **Regression safety** — Current CSV import, nesting, and `.pnest` persistence behavior cannot regress while XLSX/editing land
2. **Format parity** — Equivalent CSV and XLSX data must yield the same row payload shape, validation statuses, and actionable error/warning codes
3. **Edit persistence** — Inline import-table changes must revalidate immediately and survive save/open exactly, including deletes and validation messages
4. **Failure clarity** — Missing materials, bad numerics, corrupt workbooks, and multi-issue rows must stay user-visible and specific; no crashes, hangs, or silent drops

### Test Categories (Scaffolded):
- **XLSX Import Tests** — Required headers, column order flexibility, empty file handling, multi-sheet behavior (first sheet only), corrupt file handling, Unicode round-trip
- **File Import Dispatcher Tests** — `.csv` → `CsvImportService`, `.xlsx` → `XlsxImportService`, unknown extensions → `unsupported-file-type`
- **Part Editor Tests** — Update valid/error rows, delete with duplicate-id recalculation, add with auto-rowId assignment, non-existent rowId error, revalidate with changed material library
- **Bridge Round-Trip Tests** — `import-file` with CSV (parity), `import-file` with XLSX, `update-part-row`, `delete-part-row`, `add-part-row` response completeness
- **Integration Tests** — End-to-end: import XLSX → edit rows → revalidate → run nesting

### Open Scope Questions Resolved by Design Review:
- ✓ Phase 4 includes add-row/manual row creation (yes, via `add-part-row` message)
- ✓ Bridge introduces new generic `import-file` message (CSV backward-compatible, XLSX forward-compatible)
- ✓ No new validation rules beyond today's CSV checks
- ✓ Material-library drift after reopen stays inline error (no remap workflow)

## Deliverables Completed

1. **Phase 4 Test Matrix** — `tests/Phase4-Import-Pipeline-Test-Matrix.md`
   - Comprehensive test scaffolding aligned with Ripley's design decisions
   - Four test categories with specific scenarios per category
   - Implementation batch mapping (Batch 1 tests → contracts first, Batch 2 tests → bridge/UI parallel, Batch 3 tests → integration)

2. **Smoke Test Guide Extension** — `.squad/smoke-test-guide.md`
   - Added Phase 4 manual validation workflows
   - XLSX import scenario
   - Inline edit → revalidate → save scenario
   - Edit persistence across open-save cycle

3. **Gate Scorecard** — Phase 4 review gate logged
   - Four non-negotiables formally stated
   - Rejection criteria explicit (XLSX without edits, edits without persistence, generic failure messages)
   - Convergence target: automated Phase 4 matrix + manual smoke guide share same reviewer gate

## Outcome

**Phase 4 gate scaffolding complete.** Hicks established four non-negotiable acceptance criteria before implementation bifurcation. Test matrix, smoke guide, and explicit rejection criteria now exist as target-state documentation. Teams can implement against stable review target instead of vague "import hardening" language. Any Phase 4 batch that ships XLSX support but leaves edits non-persistent, or ships inline editing with generic failure messaging, should be rejected.
