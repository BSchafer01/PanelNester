# 2026-03-15T00:23:56Z — Ripley — FlatBuffers Design Review & Save Crash Analysis

**Status:** Complete  
**Role:** Lead (Architecture & Scope Control)

## Outcome

Specified the FlatBuffers `.pnest` file-format migration strategy, save crash root-cause analysis, schema design, seam ownership split, and implementation sequence.

### Key Decisions

1. **File Format:** 8-byte PNST header (magic + version) + FlatBuffers binary payload
   - Enables dual-read backward compatibility: format detection via magic bytes
   - Legacy JSON files open unchanged (no magic → JSON path)
   - All new saves use FlatBuffers v2 format

2. **Save Crash Root Cause:** Most likely culprit is `CaptureMaterialSnapshotsAsync` calling `ToDictionary(m => m.Name)` on a material library with duplicate names (e.g., from manual JSON edits)
   - Throws `ArgumentException` that escapes the catch handler
   - Fix required BEFORE FlatBuffers implementation (Batch 1)

3. **Seam Ownership:**
   - **Parker (Domain/Services):** Crash fix, schema, generated code, FlatBufferProjectSerializer, format-detection wrapper
   - **Bishop (Desktop bridge):** No changes required (format transparent)
   - **Hicks (Testing):** Crash reproduction, round-trip tests, precision tests, legacy migration tests
   - **Dallas (WebUI):** No changes required (format transparent)

4. **Implementation Sequence:**
   - **Batch 1:** Fix crash + write schema + generate code (Parker + Hicks parallel)
   - **Batch 2:** Implement serializers + format dispatch (Parker + Hicks parallel)
   - **Batch 3:** Integration gate (Hicks + Bishop)

## Deliverable

`.squad/decisions/inbox/ripley-flatbuffers-design-review.md` — Full design review with schema, risk analysis, type mappings, and implementation details.

## Next Steps

Parker and Hicks proceed to Batch 1 (crash fix + schema definition).
