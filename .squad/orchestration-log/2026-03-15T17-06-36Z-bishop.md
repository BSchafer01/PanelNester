# Orchestration Log — Bishop

**Date:** 2026-03-15T17:06:36Z
**Role:** Platform Dev
**Task:** Retarget application, tests, and installer from .NET 10 to .NET 8

## Delivered

1. ✅ Retargeted `PanelNester.Domain`, `PanelNester.Services`, `PanelNester.Desktop` from `net10.0` → `net8.0` (or `net8.0-windows` for WPF)
2. ✅ Retargeted all three test projects to matching .NET 8 TFMs
3. ✅ Solution builds cleanly: `dotnet build .\PanelNester.slnx -c Release --nologo`
4. ✅ Test baseline validated: `dotnet test .\PanelNester.slnx -c Release --nologo` → 134 total / 132 passed / 2 skipped / 0 failed
5. ✅ Per-user MSI rebuilds: `dotnet build .\installer\PanelNester.Installer\PanelNester.Installer.wixproj -c Release --nologo` → `PanelNester-PerUser.msi` produced
6. ✅ Installer publish runtimeconfig confirmed: `tfm: net8.0` with `Microsoft.WindowsDesktop.App 8.0.0`

## TFM Changes

| File | From | To |
|------|------|-----|
| `src/PanelNester.Domain/PanelNester.Domain.csproj` | `net10.0` | `net8.0` |
| `src/PanelNester.Services/PanelNester.Services.csproj` | `net10.0` | `net8.0` |
| `src/PanelNester.Desktop/PanelNester.Desktop.csproj` | `net10.0-windows` | `net8.0-windows` |
| `tests/PanelNester.Domain.Tests/PanelNester.Domain.Tests.csproj` | `net10.0` | `net8.0` |
| `tests/PanelNester.Services.Tests/PanelNester.Services.Tests.csproj` | `net10.0` | `net8.0` |
| `tests/PanelNester.Desktop.Tests/PanelNester.Desktop.Tests.csproj` | `net10.0-windows` | `net8.0-windows` |

## WiX Project

- No TFM changes needed in WiX because `PanelNester.Installer.wixproj` publishes `PanelNester.Desktop.csproj` directly and inherits the TFM from the project file.
- Per-user installer scope remains unchanged (`Scope="perUser"`, install to `%LocalAppData%\Programs\PanelNester`).
- WebView2 user-data relocation to `%LocalAppData%\PanelNester\WebView2\UserData` remains active and stable.

## Outcome

✅ Core retarget complete. All relevant project files moved to .NET 8. MSI rebuilt successfully. Baseline validation stayed green. Ready for acceptance review.
