# Orchestration: Bishop — Native Shell Chrome Cleanup

**Date:** 2026-03-15T02:40:13Z  
**Agent:** Bishop  
**Session ID:** scribe-ui-cleanup-batch

## Work Assignment

Clean native WPF shell chrome: remove header/footer rows from MainWindow.xaml, listen to WebView2's `DocumentTitleChanged` event, mirror `document.title` to custom titlebar and `Window.Title` property.

## Deliverables

1. **MainWindow.xaml** — Remove Row 1 (header DockPanel with title + Source badge), Remove Row 3 (footer StatusTextBlock)
2. **MainWindow.xaml.cs** — Add `UpdateWindowTitle()` method to handle `DocumentTitleChanged` events
3. **Custom titlebar sync** — Mirror document.title value to titlebar TextBlock and Window.Title property
4. **Fallback behavior** — If page title blank, use `Untitled Project — PanelNester`

## Output

- **Decision:** `.squad/decisions/inbox/bishop-ui-cleanup.md` — Decision to use WebView2.DocumentTitleChanged seam instead of new bridge message
- **Files Modified:** MainWindow.xaml, MainWindow.xaml.cs, WebViewBridge (if needed for titlebar sync)

## Architectural Insight

Chose to use existing WebView2 `DocumentTitleChanged` event instead of adding new `update-window-title` bridge message. Benefits:
- Keeps host/web boundary smaller and explicit
- Web shell remains source of truth for project identity
- Reduces regression risk in startup/loading flows
- Preserves existing bridge contract footprint

## Test Results

- `dotnet test` ✅ 132 tests passing (0 failures)
- MainWindow chrome removed ✅
- Titlebar mirrors document.title ✅
- Fallback behavior tested ✅

---

**Status:** COMPLETE. Native shell fully cleaned and approved.
