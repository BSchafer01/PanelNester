# Orchestration: Ripley — UI Cleanup Batch Design & Gating

**Date:** 2026-03-15T02:40:13Z  
**Agent:** Ripley  
**Session ID:** scribe-ui-cleanup-batch

## Work Assignment

Defined the UI Cleanup batch scope: remove native shell chrome (WPF header/footer), clean WebUI shell (remove debug sidebar + title block), add VS Code-style File menu, synchronize titlebar with project state via `document.title`, and harden navigation indicator visibility on resize.

## Output

- **Decision:** `.squad/decisions/inbox/ripley-ui-cleanup.md` — Complete split design with Dallas (WebUI) and Bishop (Desktop) seam ownership, bridge contract definition, and validation matrix
- **Status:** Proposed → Approved (post-Hicks review)

## Seam Ownership Split

| Agent | Responsibility | Files |
|-------|-----------------|-------|
| Dallas | Remove WebUI chrome (h1 from header, info sections from Overview), add File menu, implement inset nav indicator | `AppShell.tsx`, `OverviewPage.tsx`, `styles.css`, `App.tsx` document.title |
| Bishop | Remove WPF header/footer rows, listen to `CoreWebView2.DocumentTitleChanged`, mirror to titlebar | `MainWindow.xaml`, `MainWindow.xaml.cs` |
| Hicks | Validation matrix: visual inspection, titlebar tests, form persistence, existing test pass | Acceptance criteria matrix (8 checks) |

## Bridge Contract

**Message:** `update-window-title` (originally proposed, later superseded by `document.title` seam)  
**Decision reversal:** Bishop's follow-up decision rejected the new message in favor of reading `document.title` from WebView2's `DocumentTitleChanged` event—smaller vocabulary, existing event, no new bridge chatter.

## Validation Gates Cleared

1. ✅ WPF header/footer removed (visual inspection)
2. ✅ WebUI shell title/subtitle removed, File menu added
3. ✅ Right debug sidebar completely removed
4. ✅ Overview page info sections removed (Project File, Saved Snapshot, Next Save, Workflow)
5. ✅ Navigation indicator visible at all viewports (inset shadow, no clip risk)
6. ✅ Titlebar mirrors `document.title` with project name + dirty asterisk
7. ✅ Existing tests pass (132 total, 0 failures)
8. ✅ No console errors or build regressions

## Next Steps

- Merge decisions inbox files to `decisions.md`
- Append Ripley UI Cleanup entry to team history
- Commit `.squad/` changes

---

**Status:** APPROVED for merge. Phase 6 includes UI cleanup in hardening batch.
