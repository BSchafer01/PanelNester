# 2026-03-15T00:23:56Z — Hicks — FlatBuffers Save Gate & Migration Test Plan

**Status:** Complete  
**Role:** Tester (Acceptance & Regression Coverage)

## Outcome

Specified dual-read migration gate for `.pnest` format transition and the test coverage required to ensure no silent regressions when existing JSON files are opened and re-saved.

### Key Decisions

1. **Dual-Read Transition Gate:** No format incompatibility regressions
   - Old JSON `.pnest` files must still open and work correctly
   - Newly saved `.pnest` files must be FlatBuffers binary (not JSON text)
   - Can be verified by format detection (magic bytes)

2. **Migration Test Coverage:**
   - Save crash reproduction test (before fix applied)
   - Legacy JSON load tests (existing `.pnest` files still open)
   - FlatBuffers round-trip tests (save → load → equivalence)
   - Precision tests (decimal ↔ double conversions)
   - Edge cases (empty projects, large nesting results, null results)
   - Cancelled/failed save non-destructiveness (no data corruption on next save)

3. **Integration Gate Phases:**
   - **Phase 1 (Batch 1):** Save crash reproduction + fix verification
   - **Phase 2 (Batch 2):** Round-trip, precision, edge-case tests
   - **Phase 3 (Batch 3):** Full integration pass (new → save → open → edit → save)

## Deliverable

`.squad/decisions/inbox/hicks-flatbuffers-gate.md` — Gate acceptance criteria and test matrix.

## Next Steps

Hicks awaits Parker's Batch 1 delivery (crash fix + schema) before writing crash reproduction test.
