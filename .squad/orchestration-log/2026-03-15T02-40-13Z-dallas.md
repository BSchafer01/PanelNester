# Orchestration: Dallas — WebUI Chrome Cleanup

**Date:** 2026-03-15T02:40:13Z  
**Agent:** Dallas  
**Session ID:** scribe-ui-cleanup-batch

## Work Assignment

Clean WebUI shell chrome: remove AppShell header title block, delete Overview page debug sections, implement VS Code-style File menu, fix active navigation indicator clipping with inset box-shadow, set `document.title` as titlebar source.

## Deliverables

1. **AppShell.tsx** — Remove h1/subtitle/phase tags; add File menu dropdown with New/Open/Save/Save As
2. **OverviewPage.tsx** — Remove "Project File" and "Workflow" informational sections
3. **styles.css** — Remove sidebar CSS (.app-shell__sidebar, .panel, .token-list, .token, .activity-list, .eyebrow); update grid layout
4. **App.tsx** — Set `document.title` to `{projectName}{isDirty ? ' *' : ''} — PanelNester`
5. **Navigation indicator fix** — Use `box-shadow: inset 2px 0 0 accent` instead of offset border to prevent clipping at narrow widths

## Output

- **Decision:** `.squad/decisions/inbox/dallas-ui-cleanup.md` — Decision rationale and consequences
- **Decision:** `.squad/decisions/inbox/dallas-ui-cleanup-followup.md` — Preserved metadata form, removed info panels below it
- **Files Modified:** AppShell.tsx, OverviewPage.tsx, styles.css, App.tsx

## Test Results

- `npm run build` ✅ passed
- 132 tests passing (0 failures, 2 skipped)
- Navigation indicator visible at 320px–1440px ✅
- File menu operations quiet ✅

## Follow-up Corrections

Removed dedicated `Saved snapshot` and `Next save` panels below metadata form to clean informational clutter, keeping saved snapshot count in summary card (outside metadata section).

---

**Status:** COMPLETE. WebUI shell fully cleaned and approved.
