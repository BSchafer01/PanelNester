# Orchestration: Ripley — Per-User MSI Revision & Revalidation

**Date:** 2026-03-15T16:39:48Z  
**Agent:** Ripley  
**Session ID:** scribe-per-user-msi-packaging

## Work Assignment

Revise MSI/install behavior so WebView2 user data lives outside the install directory and rerun trust validation. Hicks' rejection blocked Bishop from next revision cycle; Ripley authorized to revise artifact and unblock gate.

### Decision & Implementation

- **Decision:** `.squad/decisions/inbox/ripley-per-user-msi-revision.md` — WebView2 profile must resolve to `%LOCALAPPDATA%\PanelNester\WebView2\UserData`, not default `*.exe.WebView2` under install folder
- **Root Cause:** Per-user MSI lifecycle untrustworthy if install root mutates on launch and leaves residue after uninstall
- **Implementation:**
  1. `DesktopStoragePaths` now owns both app-data root and WebView2 profile path
  2. `WebViewBridge.InitializeAsync` creates explicit `CoreWebView2Environment` with dedicated user-data folder
  3. `MainWindow` passes path into bridge during startup
  4. Installer scope unchanged (per-user/non-admin) — revision is host behavior only

### Validation

- ✅ `dotnet build .\PanelNester.slnx -c Release --nologo`
- ✅ `dotnet test .\PanelNester.slnx -c Release --nologo` → 134 total, 132 passed, 2 skipped, 0 failed
- ✅ `npm run build` in `src\PanelNester.WebUI`
- ✅ `dotnet build .\installer\PanelNester.Installer\PanelNester.Installer.wixproj -c Release --nologo`
- ✅ Silent install → first launch (10 sec smoke) → silent uninstall
- ✅ **No** `PanelNester.Desktop.exe.WebView2` under `%LOCALAPPDATA%\Programs\PanelNester` after launch
- ✅ **Yes** `%LOCALAPPDATA%\PanelNester\WebView2\UserData` created and persisted
- ✅ Install root removed post-uninstall; WebView2 user data retained as app state

### Status

✅ **Complete & Revalidated** — MSI revised; installer lifecycle clean. Ready for Hicks final re-review.

---

**Hicks Final Re-Review Verdict:** APPROVED ✅ (2026-03-15T16:39:48Z)
