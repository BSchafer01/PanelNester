# Orchestration Log — Hicks

**Date:** 2026-03-15T17:06:36Z
**Role:** Tester
**Task:** Define .NET 8 downgrade gate, review first pass, reject on stale validation docs, then re-review after revision

## First Review

**Date:** 2026-03-15 (first pass)
**Verdict:** REJECTED ❌

**Reason:** Executable checks moved to .NET 8, but reviewer-facing validation docs still asserted `.NET 10-windows` target.

**Blocking Issue:**
- `tests\Phase0-1-Test-Matrix.md` line 15 stated desktop host should target `net10.0-windows`
- Hicks gate explicitly required authored test/doc files with hardcoded TFM literals to move with project retarget
- This mismatch would mislead future reviewers and implementers

**Passed Checks:**
- All six project TFMs correctly moved to .NET 8 / `net8.0-windows`
- Desktop/WPF contract still valid on .NET 8 Windows
- Per-user MSI remained non-admin
- Regression baseline green (134 total / 132 passed / 2 skipped / 0 failed)
- Runtime prerequisites implicit but present

## Final Review

**Date:** 2026-03-15 (after Ripley revision)
**Verdict:** APPROVED ✅

**Evidence:**
1. ✅ All six TFMs aligned on .NET 8
2. ✅ Desktop/WPF contract valid (`UseWPF=true`, runtimeconfig reports `net8.0`)
3. ✅ Active validation docs corrected:
   - `tests\Phase0-1-Test-Matrix.md` now states `net8.0-windows`
   - `.squad\smoke-test-guide.md` now requires `.NET 8.0.x` SDK and `.NET 8 Desktop Runtime`
4. ✅ Baseline validation green after retarget
5. ✅ Runtime prerequisites explicit and documented

## Gate Design

Five non-negotiable pass conditions defined:
1. All relevant TFMs move together (Domain + Services + tests, Desktop + desktop tests)
2. Desktop/WPF contract stays valid on .NET 8 Windows
3. Per-user MSI remains non-admin
4. Regression baseline stays green
5. Runtime prerequisites are explicit (`.NET 8 Desktop Runtime x64` + `WebView2`)

## Outcome

✅ Gate authored. First review rejected on active-docs mismatch (expected). Final review approved after Ripley corrected stale validation artifacts. Framework retarget complete and validated.
